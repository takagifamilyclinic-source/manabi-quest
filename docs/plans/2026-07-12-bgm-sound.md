# BGM／効果音 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** まなびクエストに、著作権フリーの完全オリジナルBGM3曲＋効果音を Web Audio で合成して追加する（課金ゼロ・オフライン・iPad対応）。

**Architecture:** 責務分離の3モジュール。`js/audio-util.js`（純ロジック・node --testで検証）／`js/audio-data.js`（曲・SEデータ＝作曲）／`js/audio.js`（AudioContextエンジン・ブラウザ専用）。`js/app.js` の既存 `show()`／正誤ハンドラ等に最小限フックし、端末単位設定は `localStorage['manabi-audio']` に保存（stateスキーマ非変更）。

**Tech Stack:** buildless バニラJS（ESモジュール・依存ゼロ）、Web Audio API、node:test、PWA(Service Worker)。

## Global Constraints
- **課金ゼロ**: 音声ファイル同梱・外部音源・音ライブラリ・有料API 一切不可。音は Web Audio で合成。
- **著作権**: 実在曲の旋律を写さない。完全オリジナル。
- **依存ゼロ / buildless**: npm依存やバンドラを追加しない。ESモジュールのまま。
- **スキーマ非変更**: 子の進捗 state（v5）に触れない。音設定は別キー `manabi-audio`。
- **テスト**: `npm test`（= `node --test tests/*.test.mjs`）。音の実体はブラウザ＋実iPadで確認。
- **JS変更時は `sw.js` の CACHE を上げる**（現行 `manabi-quest-v1.10.0` → `v1.11.0`）。
- ブランチ `feat/bgm` で作業（既に作成済み）。相対パス配信。

---

### Task 1: 純ロジック `js/audio-util.js`

**Files:**
- Create: `js/audio-util.js`
- Test: `tests/audio-util.test.mjs`

**Interfaces:**
- Produces:
  - `noteToFreq(note: string): number` — 音名→周波数(Hz)。`'A4'→440`、`'R'`/不正→`0`。
  - `trackForScreen(screenId: string): 'field'|'battle'|null` — 画面ID→ループ曲名。未知→`null`。（result は `'field'`。勝利ジングルは呼び出し側で別途 `playJingle`。）
  - `clampVolume(x: number): number` — 有限数を`[0,1]`に丸める。非有限は`0.7`。
  - `parseAudioSettings(raw: string|null): {muted:boolean, volume:number}` — 既定`{muted:false, volume:0.7}`。

- [ ] **Step 1: Write the failing test**

Create `tests/audio-util.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  noteToFreq,
  trackForScreen,
  clampVolume,
  parseAudioSettings,
} from "../js/audio-util.js";

test("noteToFreq: A4 は 440Hz", () => {
  assert.ok(Math.abs(noteToFreq("A4") - 440) < 0.001);
});

test("noteToFreq: 中央ドC4 は約261.63Hz", () => {
  assert.ok(Math.abs(noteToFreq("C4") - 261.626) < 0.01);
});

test("noteToFreq: シャープと1オクターブ上", () => {
  assert.ok(Math.abs(noteToFreq("A5") - 880) < 0.001);
  assert.ok(Math.abs(noteToFreq("A#4") - 466.164) < 0.01);
});

test("noteToFreq: 休符Rと不正入力は0", () => {
  assert.equal(noteToFreq("R"), 0);
  assert.equal(noteToFreq(""), 0);
  assert.equal(noteToFreq("H9"), 0);
  assert.equal(noteToFreq(null), 0);
});

test("trackForScreen: バトルはbattle、他の主要画面はfield", () => {
  assert.equal(trackForScreen("#screen-battle"), "battle");
  for (const id of [
    "#screen-home",
    "#screen-subject",
    "#screen-zukan",
    "#screen-badges",
    "#screen-profile",
    "#screen-parent",
    "#screen-reward",
    "#screen-result",
  ]) {
    assert.equal(trackForScreen(id), "field", id);
  }
});

test("trackForScreen: 未知IDはnull", () => {
  assert.equal(trackForScreen("#screen-unknown"), null);
  assert.equal(trackForScreen(""), null);
});

test("clampVolume: 範囲・境界・非有限", () => {
  assert.equal(clampVolume(0.5), 0.5);
  assert.equal(clampVolume(-1), 0);
  assert.equal(clampVolume(2), 1);
  assert.equal(clampVolume(0), 0);
  assert.equal(clampVolume(1), 1);
  assert.equal(clampVolume(NaN), 0.7);
  assert.equal(clampVolume("x"), 0.7);
});

test("parseAudioSettings: 正常・欠損・壊れJSON・範囲外", () => {
  assert.deepEqual(parseAudioSettings('{"muted":true,"volume":0.3}'), {
    muted: true,
    volume: 0.3,
  });
  assert.deepEqual(parseAudioSettings(null), { muted: false, volume: 0.7 });
  assert.deepEqual(parseAudioSettings("not json"), {
    muted: false,
    volume: 0.7,
  });
  assert.deepEqual(parseAudioSettings('{"muted":1}'), {
    muted: true,
    volume: 0.7,
  });
  assert.deepEqual(parseAudioSettings('{"volume":5}'), {
    muted: false,
    volume: 1,
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /d/Desktop-Archive/manabi-quest && node --test tests/audio-util.test.mjs`
Expected: FAIL（`Cannot find module '../js/audio-util.js'`）

- [ ] **Step 3: Write minimal implementation**

Create `js/audio-util.js`:

```js
// 純ロジック（AudioContext/localStorage に触れない・node --test 対象）
const NOTE_INDEX = {
  C: 0, "C#": 1, D: 2, "D#": 3, E: 4, F: 5,
  "F#": 6, G: 7, "G#": 8, A: 9, "A#": 10, B: 11,
};

// 音名(例 "A4","C#5") → 周波数Hz。休符/不正は 0。A4=440基準・等分平均律。
export function noteToFreq(note) {
  if (typeof note !== "string" || note === "R") return 0;
  const m = /^([A-G]#?)(-?\d+)$/.exec(note);
  if (!m) return 0;
  const midi = NOTE_INDEX[m[1]] + (parseInt(m[2], 10) + 1) * 12; // A4 => 69
  return 440 * Math.pow(2, (midi - 69) / 12);
}

const FIELD_SCREENS = new Set([
  "#screen-home", "#screen-subject", "#screen-zukan", "#screen-badges",
  "#screen-profile", "#screen-parent", "#screen-reward", "#screen-result",
]);

// 画面ID → ループ曲名。未知は null（曲を変えない）。
export function trackForScreen(screenId) {
  if (screenId === "#screen-battle") return "battle";
  if (FIELD_SCREENS.has(screenId)) return "field";
  return null;
}

// 音量を [0,1] に丸める。非有限は既定 0.7。
export function clampVolume(x) {
  const n = Number(x);
  if (!Number.isFinite(n)) return 0.7;
  return Math.min(1, Math.max(0, n));
}

// localStorage文字列を安全にパース。壊れていれば既定値。
export function parseAudioSettings(raw) {
  const def = { muted: false, volume: 0.7 };
  if (!raw) return { ...def };
  try {
    const o = JSON.parse(raw);
    return { muted: !!o.muted, volume: clampVolume(o.volume) };
  } catch {
    return { ...def };
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/audio-util.test.mjs`
Expected: PASS（全ケース）

- [ ] **Step 5: Commit**

```bash
git add js/audio-util.js tests/audio-util.test.mjs
git commit -m "feat: audio-util 純ロジック(noteToFreq/trackForScreen/clampVolume/parseAudioSettings)"
```

---

### Task 2: 曲・SEデータ `js/audio-data.js`

**Files:**
- Create: `js/audio-data.js`
- Test: `tests/audio-data.test.mjs`

**Interfaces:**
- Consumes: `noteToFreq` from `js/audio-util.js`（テストでの妥当性検証に使用）。
- Produces:
  - `TRACKS: { field:Track, battle:Track, victory:Track }`
    - `Track = { bpm:number, loop:boolean, voices: { melody:Note[], harmony?:Note[], bass?:Note[] } }`
    - `Note = { n:string, d:number }`（`n`＝音名 or `'R'`、`d`＝拍数 beats）
  - `SFX: { correct:Note[], wrong:Note[], capture:Note[], button:Note[] }`（`d` は秒。エンジンは beatSec=1 で再生）

- [ ] **Step 1: Write the failing test**

Create `tests/audio-data.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { TRACKS, SFX } from "../js/audio-data.js";
import { noteToFreq } from "../js/audio-util.js";

function checkSeq(seq, label) {
  assert.ok(Array.isArray(seq) && seq.length > 0, `${label} は非空配列`);
  for (const ev of seq) {
    assert.ok(typeof ev.n === "string", `${label} n は文字列`);
    assert.ok(typeof ev.d === "number" && ev.d > 0, `${label} d>0`);
    if (ev.n !== "R") {
      assert.ok(noteToFreq(ev.n) > 0, `${label} 音名 ${ev.n} は解釈可能`);
    }
  }
}

test("TRACKS: 3曲そろっている", () => {
  assert.deepEqual(Object.keys(TRACKS).sort(), ["battle", "field", "victory"]);
});

test("TRACKS: 各曲の構造・音名・拍が妥当", () => {
  for (const [name, tr] of Object.entries(TRACKS)) {
    assert.ok(tr.bpm > 0, `${name} bpm>0`);
    assert.equal(typeof tr.loop, "boolean", `${name} loop は真偽`);
    assert.ok(tr.voices && tr.voices.melody, `${name} melody 必須`);
    for (const [vn, seq] of Object.entries(tr.voices)) {
      checkSeq(seq, `${name}.${vn}`);
    }
  }
});

test("TRACKS: field/battle はループ、victory はワンショット", () => {
  assert.equal(TRACKS.field.loop, true);
  assert.equal(TRACKS.battle.loop, true);
  assert.equal(TRACKS.victory.loop, false);
});

test("TRACKS: victory は短い（全声部の最長 < 4秒）", () => {
  const tr = TRACKS.victory;
  const beatSec = 60 / tr.bpm;
  const longest = Math.max(
    ...Object.values(tr.voices).map((seq) =>
      seq.reduce((s, ev) => s + ev.d, 0) * beatSec
    )
  );
  assert.ok(longest < 4, `victory 尺 ${longest.toFixed(2)}s < 4s`);
});

test("SFX: 4種そろい・妥当", () => {
  assert.deepEqual(
    Object.keys(SFX).sort(),
    ["button", "capture", "correct", "wrong"]
  );
  for (const [name, seq] of Object.entries(SFX)) checkSeq(seq, `SFX.${name}`);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/audio-data.test.mjs`
Expected: FAIL（`Cannot find module '../js/audio-data.js'`）

- [ ] **Step 3: Write minimal implementation**

Create `js/audio-data.js`（すべてオリジナル旋律・長調ベースで明るく）:

```js
// 曲・効果音データ（作曲はこのファイルだけ触る）。
// Track: { bpm, loop, voices:{ melody, harmony?, bass? } }
// Note:  { n: 音名 or 'R'(休符), d: 拍数(beats) }
// 各声部は d の合計を同じにして、独立ループしても揃うようにする。

// フィールド曲: 明るく穏やかな冒険（Cメジャー・8拍ループ）
const field = {
  bpm: 120,
  loop: true,
  voices: {
    melody: [
      { n: "G4", d: 0.5 }, { n: "C5", d: 0.5 }, { n: "E5", d: 0.5 }, { n: "C5", d: 0.5 },
      { n: "D5", d: 1 }, { n: "E5", d: 1 },
      { n: "A4", d: 0.5 }, { n: "C5", d: 0.5 }, { n: "F5", d: 0.5 }, { n: "E5", d: 0.5 },
      { n: "D5", d: 1 }, { n: "C5", d: 1 },
    ], // 合計 8拍
    harmony: [
      { n: "E4", d: 2 }, { n: "G4", d: 2 }, { n: "F4", d: 2 }, { n: "E4", d: 2 },
    ], // 8拍
    bass: [
      { n: "C3", d: 1 }, { n: "C3", d: 1 }, { n: "G2", d: 1 }, { n: "G2", d: 1 },
      { n: "F2", d: 1 }, { n: "F2", d: 1 }, { n: "G2", d: 1 }, { n: "C3", d: 1 },
    ], // 8拍
  },
};

// バトル曲: 少し速くわくわく疾走（Aマイナー・8拍ループ）
const battle = {
  bpm: 160,
  loop: true,
  voices: {
    melody: [
      { n: "A4", d: 0.5 }, { n: "A4", d: 0.5 }, { n: "C5", d: 0.5 }, { n: "B4", d: 0.5 },
      { n: "A4", d: 0.5 }, { n: "E5", d: 0.5 }, { n: "A4", d: 1 },
      { n: "F5", d: 0.5 }, { n: "E5", d: 0.5 }, { n: "D5", d: 0.5 }, { n: "C5", d: 0.5 },
      { n: "B4", d: 0.5 }, { n: "C5", d: 0.5 }, { n: "A4", d: 1 },
    ], // 8拍
    harmony: [
      { n: "A3", d: 2 }, { n: "F3", d: 2 }, { n: "D3", d: 2 }, { n: "E3", d: 2 },
    ], // 8拍
    bass: [
      { n: "A2", d: 1 }, { n: "A2", d: 1 }, { n: "F2", d: 1 }, { n: "F2", d: 1 },
      { n: "D2", d: 1 }, { n: "D2", d: 1 }, { n: "E2", d: 1 }, { n: "E2", d: 1 },
    ], // 8拍
  },
};

// 勝利ジングル: 短い上昇の高揚（ワンショット・約1.2秒）
const victory = {
  bpm: 140,
  loop: false,
  voices: {
    melody: [
      { n: "C5", d: 0.25 }, { n: "E5", d: 0.25 }, { n: "G5", d: 0.25 },
      { n: "C6", d: 0.75 }, { n: "G5", d: 0.25 }, { n: "C6", d: 1 },
    ], // 2.75拍
    bass: [
      { n: "C3", d: 1.5 }, { n: "G2", d: 0.5 }, { n: "C3", d: 0.75 },
    ], // 2.75拍
  },
};

export const TRACKS = { field, battle, victory };

// 効果音（d は秒。エンジンは beatSec=1 で再生）
export const SFX = {
  correct: [{ n: "E5", d: 0.08 }, { n: "A5", d: 0.14 }], // 上昇2音 ピロン♪
  wrong: [{ n: "A3", d: 0.1 }, { n: "E3", d: 0.18 }], // 下降 ブブッ
  capture: [
    { n: "C5", d: 0.09 }, { n: "E5", d: 0.09 }, { n: "G5", d: 0.09 }, { n: "C6", d: 0.22 },
  ], // 上昇アルペジオ ファンファーレ
  button: [{ n: "C6", d: 0.05 }], // ポッ
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/audio-data.test.mjs`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add js/audio-data.js tests/audio-data.test.mjs
git commit -m "feat: audio-data オリジナル3曲+効果音データ"
```

---

### Task 3: エンジン `js/audio.js`（ブラウザ専用）

node で音は鳴らせないため、このタスクの検証は **ブラウザのスモークテスト**（chrome-devtools MCP）で行う。純ロジックは Task 1/2 で担保済み。

**Files:**
- Create: `js/audio.js`

**Interfaces:**
- Consumes: `TRACKS`, `SFX` from `js/audio-data.js`／`noteToFreq`, `clampVolume`, `parseAudioSettings` from `js/audio-util.js`。
- Produces（`audio` シングルトンの公開API・app.js が使う）:
  - `audio.init(): void` — 最初のユーザー操作で遅延生成＋`resume()`。多重呼び出し安全。
  - `audio.playTrack(name: 'field'|'battle'): void` — ループ曲。再生中の同名は無視。
  - `audio.playJingle(name: 'victory', onDone?: ()=>void): void` — ワンショット。終了後 `onDone` を呼ぶ。
  - `audio.stopTrack(): void` — 現在のループ/ジングルを停止。
  - `audio.playSfx(name: 'correct'|'wrong'|'capture'|'button'): void`
  - `audio.setMuted(b: boolean): void` / `audio.isMuted(): boolean`
  - `audio.setVolume(v: number): void` / `audio.getVolume(): number`

- [ ] **Step 1: Write the implementation**

Create `js/audio.js`（この内容をそのまま使う。関数の重複はなし）:

```js
import { TRACKS, SFX } from "./audio-data.js";
import { clampVolume, noteToFreq, parseAudioSettings } from "./audio-util.js";

const STORAGE_KEY = "manabi-audio";
// 声部名 → 波形（melody=8bit矩形波、伴奏/ベース=丸い三角波、SFX=矩形波）
const WAVE = { melody: "square", harmony: "triangle", bass: "triangle", sfx: "square" };
const LOOKAHEAD = 0.2; // 秒: 先読み
const TICK = 25; // ms: スケジューラ間隔

function acCtor() {
  return typeof window !== "undefined" &&
    (window.AudioContext || window.webkitAudioContext);
}

let ctx = null;
let master = null; // 音量ノード
let musicGain = null;
let sfxGain = null;
let settings = parseAudioSettings(
  typeof localStorage !== "undefined" ? localStorage.getItem(STORAGE_KEY) : null
);
let current = null; // 現在のループ曲名 or null
let timer = null;
let voiceState = null; // { [voice]: {seq, idx, time, beatSec, wave, dest} }
let jingleTimeout = null;

function persist() {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(settings));
  } catch {}
}

function applyVolume() {
  if (master) master.gain.value = settings.muted ? 0 : settings.volume;
}

function ensureCtx() {
  if (ctx || !acCtor()) return;
  const AC = acCtor();
  ctx = new AC();
  master = ctx.createGain();
  applyVolume();
  master.connect(ctx.destination);
  musicGain = ctx.createGain();
  musicGain.gain.value = 0.5;
  musicGain.connect(master);
  sfxGain = ctx.createGain();
  sfxGain.gain.value = 0.9;
  sfxGain.connect(master);
}

// 1音を at 秒から dur 秒ぶん、簡易ADSRで鳴らす
function playNote(wave, freq, at, dur, dest) {
  if (!ctx || freq <= 0) return;
  const o = ctx.createOscillator();
  o.type = wave;
  o.frequency.value = freq;
  const g = ctx.createGain();
  const a = 0.008;
  const r = Math.min(0.06, dur * 0.4);
  const peak = 0.9;
  const sus = 0.6;
  const relStart = Math.max(at + a + 0.02, at + dur - r);
  g.gain.setValueAtTime(0, at);
  g.gain.linearRampToValueAtTime(peak, at + a);
  g.gain.linearRampToValueAtTime(sus, at + a + 0.03);
  g.gain.setValueAtTime(sus, relStart);
  g.gain.linearRampToValueAtTime(0.0001, at + dur);
  o.connect(g);
  g.connect(dest);
  o.start(at);
  o.stop(at + dur + 0.03);
}

function stopScheduler() {
  if (timer) {
    clearInterval(timer);
    timer = null;
  }
  if (jingleTimeout) {
    clearTimeout(jingleTimeout);
    jingleTimeout = null;
  }
  voiceState = null;
}

// 曲を開始（loop=true でループ、false でワンショット）
function startSong(name, loop) {
  const tr = TRACKS[name];
  if (!tr || !ctx) return;
  stopScheduler();
  const beatSec = 60 / tr.bpm;
  const startAt = ctx.currentTime + 0.06;
  voiceState = {};
  for (const [vn, seq] of Object.entries(tr.voices)) {
    voiceState[vn] = {
      seq,
      idx: 0,
      time: startAt,
      beatSec,
      wave: WAVE[vn] || "square",
      dest: musicGain,
    };
  }
  timer = setInterval(() => scheduleTick(loop), TICK);
  scheduleTick(loop);
}

function scheduleTick(loop) {
  if (!ctx || !voiceState) return;
  const horizon = ctx.currentTime + LOOKAHEAD;
  for (const st of Object.values(voiceState)) {
    while (st.time < horizon) {
      if (st.idx >= st.seq.length) {
        if (loop) st.idx = 0;
        else break;
      }
      const ev = st.seq[st.idx];
      const dur = ev.d * st.beatSec;
      if (ev.n !== "R") {
        playNote(st.wave, noteToFreq(ev.n), st.time, dur, st.dest);
      }
      st.time += dur;
      st.idx++;
    }
  }
}

function songDuration(name) {
  const tr = TRACKS[name];
  const beatSec = 60 / tr.bpm;
  return Math.max(
    ...Object.values(tr.voices).map(
      (seq) => seq.reduce((s, ev) => s + ev.d, 0) * beatSec
    )
  );
}

export const audio = {
  init() {
    ensureCtx();
    if (ctx && ctx.state === "suspended") ctx.resume();
  },
  playTrack(name) {
    ensureCtx();
    if (!ctx) return;
    if (current === name && timer) return; // 既に同じ曲が再生中
    current = name;
    startSong(name, true);
  },
  playJingle(name, onDone) {
    ensureCtx();
    if (!ctx) {
      if (onDone) onDone();
      return;
    }
    current = null; // ループ扱いを解除
    startSong(name, false);
    jingleTimeout = setTimeout(() => {
      stopScheduler();
      if (onDone) onDone();
    }, (songDuration(name) + 0.1) * 1000);
  },
  stopTrack() {
    current = null;
    stopScheduler();
  },
  playSfx(name) {
    ensureCtx();
    if (!ctx) return;
    const seq = SFX[name];
    if (!seq) return;
    let t = ctx.currentTime + 0.02;
    for (const ev of seq) {
      if (ev.n !== "R") playNote(WAVE.sfx, noteToFreq(ev.n), t, ev.d, sfxGain);
      t += ev.d; // SFX の d は秒（beatSec=1）
    }
  },
  setMuted(b) {
    settings.muted = !!b;
    applyVolume();
    persist();
  },
  isMuted() {
    return settings.muted;
  },
  setVolume(v) {
    settings.volume = clampVolume(v);
    applyVolume();
    persist();
  },
  getVolume() {
    return settings.volume;
  },
};
```

- [ ] **Step 2: Browser smoke test（chrome-devtools MCP）**

ローカルサーバを起動してブラウザで確認する。一時ページに検証スクリプトを仕込む。

Create `_audio_smoke.html`（プロジェクト直下・検証後に削除）:

```html
<!doctype html><meta charset="utf-8"><body>
<button id="go">start</button>
<script type="module">
import { audio } from "./js/audio.js";
import { noteToFreq } from "./js/audio-util.js";
window.__r = { noteA4: noteToFreq("A4") };
document.getElementById("go").onclick = () => {
  audio.init();
  audio.playTrack("field");
  setTimeout(() => audio.playSfx("correct"), 800);
  setTimeout(() => audio.playTrack("battle"), 1600);
  setTimeout(() => audio.playJingle("victory", () => audio.playTrack("field")), 3000);
  window.__r.muted = audio.isMuted();
  window.__r.vol = audio.getVolume();
  window.__r.ok = true;
};
</script></body>
```

Run server: `cd /d/Desktop-Archive/manabi-quest && python -m http.server 8765`（バックグラウンド）
- chrome-devtools MCP で `http://localhost:8765/_audio_smoke.html` を開く → `#go` をクリック → コンソールエラーが出ないこと、`window.__r.ok === true`・`noteA4≈440` を `evaluate_script` で確認。
- 期待: 例外なし。曲切替・SE・ジングル→field復帰が動く（音は実機で実聴確認）。

- [ ] **Step 3: 一時ファイル削除**

```bash
rm -f /d/Desktop-Archive/manabi-quest/_audio_smoke.html
```

- [ ] **Step 4: Commit**

```bash
git add js/audio.js
git commit -m "feat: audio エンジン(Web Audio先読みスケジューラ・3曲+SE+ミュート/音量)"
```

---

### Task 4: app.js 組み込み ＋ CSS（🔊トグル・音量スライダー）

**Files:**
- Modify: `js/app.js`（フック6点・現在画面IDの記録）
- Modify: `css/style.css`（トグルボタン・音量スライダー）

**Interfaces:**
- Consumes: `audio` from `js/audio.js`／`trackForScreen` from `js/audio-util.js`。

- [ ] **Step 1: import と初回タップ解除・画面追従を追加**

`js/app.js` の先頭 import 群に追加:

```js
import { audio } from "./audio.js";
import { trackForScreen } from "./audio-util.js";
```

`js/app.js` の既存 `show()` を特定する（`grep -n "function show" js/app.js`）。`show(sel)` の実装内、画面表示処理の直後に曲追従を足し、現在画面IDを記録する。例（既存 `show` に合わせて統合。現在の実装が `.screen` の active 切替なら、その最後に以下を追加）:

```js
let __currentScreen = null;
function applyScreenTrack(sel) {
  __currentScreen = sel;
  const t = trackForScreen(sel);
  if (t) audio.playTrack(t);
}
```

`show()` の末尾に `applyScreenTrack(sel);` を1行追加（`sel` は `show` の引数名に合わせる。例: `show("#screen-home")` なら `sel==="#screen-home"`）。

ファイル末尾（初期化コード付近）に、初回タップ解除と省電力を追加:

```js
// 最初のユーザー操作でオーディオを解錠（自動再生ブロック対策）
function unlockAudioOnce() {
  audio.init();
  if (__currentScreen) {
    const t = trackForScreen(__currentScreen);
    if (t) audio.playTrack(t);
  }
  window.removeEventListener("pointerdown", unlockAudioOnce);
}
window.addEventListener("pointerdown", unlockAudioOnce);

// 省電力: タブ非表示で停止、復帰で現在画面の曲を再開
document.addEventListener("visibilitychange", () => {
  if (document.hidden) {
    audio.stopTrack();
  } else if (__currentScreen) {
    const t = trackForScreen(__currentScreen);
    if (t) audio.playTrack(t);
  }
});
```

- [ ] **Step 2: バトルの正誤・捕獲・勝利にSE/ジングルを足す**

`renderQuestion()` の解答ハンドラ（正誤判定の箇所・`grep -n "correct\|正解\|判定" js/app.js` で特定）に:

```js
// 正解時
audio.playSfx("correct");
// 不正解時
audio.playSfx("wrong");
```

`renderResult()`（`grep -n "function renderResult" js/app.js`）で、クリア（勝利＝モンスター捕獲成立）時に、`show("#screen-result")` の後で:

```js
audio.playSfx("capture");
audio.playJingle("victory", () => audio.playTrack("field"));
```

（`show("#screen-result")` が `applyScreenTrack` 経由で一瞬 field を鳴らすが、直後の `playJingle` が停止して勝利ジングル→終了後 field 復帰するので問題ない。）

- [ ] **Step 3: ホームに🔊/🔇トグルを追加**

`renderHome()`（`grep -n "function renderHome" js/app.js`）のテンプレートに、画面角のボタンを追加:

```js
// renderHome のテンプレート文字列内、ホーム画面の適所に:
`<button class="sound-toggle" id="soundToggle" aria-label="おと">${
  audio.isMuted() ? "🔇" : "🔊"
}</button>`
```

`renderHome()` の `show(...)` 後（イベント付与箇所）に:

```js
const st = document.querySelector("#soundToggle");
if (st) {
  st.addEventListener("click", () => {
    audio.setMuted(!audio.isMuted());
    st.textContent = audio.isMuted() ? "🔇" : "🔊";
  });
}
```

- [ ] **Step 4: 親ページに音量スライダーを追加**

`renderParentDash()`（`grep -n "function renderParentDash" js/app.js`）のテンプレートに:

```js
`<div class="vol-row">
  <span>おんりょう</span>
  <input type="range" id="volRange" min="0" max="100" value="${Math.round(
    audio.getVolume() * 100
  )}">
</div>`
```

`renderParentDash()` の `show(...)` 後に:

```js
const vr = document.querySelector("#volRange");
if (vr) {
  vr.addEventListener("input", () => audio.setVolume(Number(vr.value) / 100));
}
```

- [ ] **Step 5: CSS を追加**

`css/style.css` の末尾に:

```css
.sound-toggle {
  position: absolute;
  top: 10px;
  right: 10px;
  font-size: 1.5rem;
  background: rgba(255, 255, 255, 0.15);
  border: none;
  border-radius: 999px;
  width: 44px;
  height: 44px;
  cursor: pointer;
}
.vol-row {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 10px 0;
}
.vol-row input[type="range"] {
  flex: 1;
}
```

> `renderHome` の親要素が `position: static` の場合、`.sound-toggle` の絶対配置基準にするため、ホーム画面のルート要素に `position: relative;` を付ける（`#screen-home` か直下のラッパーに）。既存CSSを確認して適用。

- [ ] **Step 6: 全テストが緑のまま & ブラウザ確認**

Run: `npm test`
Expected: 既存146 + 新規（audio-util/audio-data）= 全PASS（app.js/CSSはUIのため件数に影響しない範囲で緑維持）

ブラウザ（chrome-devtools MCP・ローカルサーバ）で目視/実聴の確認項目:
- 最初のタップで曲が鳴る／ホームでフィールド曲、バトルでバトル曲、クリアで勝利ジングル→field復帰。
- 正解ピロン♪／不正解ブブッ／捕獲ファンファーレ／ボタン音。
- 🔊トグルで即ミュート・アイコン切替、リロード後も維持。親ページ音量スライダーが効く。
- 英語🔊読み上げ中も破綻しない。

- [ ] **Step 7: Commit**

```bash
git add js/app.js css/style.css
git commit -m "feat: app.jsにBGM/SE組込(画面追従・初回解錠・正誤/捕獲/勝利・🔊トグル・音量)"
```

---

### Task 5: sw更新・最終検証・公開

**Files:**
- Modify: `sw.js`（precache に3ファイル追加・CACHE 版上げ）
- Modify: `README.md`・`docs/進捗.md`（追記）

- [ ] **Step 1: sw.js の precache と CACHE 版を更新**

`sw.js` の `ASSETS` 配列（`./js/badges.js` の並び付近）に追加:

```js
  "./js/audio-util.js",
  "./js/audio-data.js",
  "./js/audio.js",
```

`sw.js` の CACHE 定義を更新:

```js
const CACHE = "manabi-quest-v1.11.0";
```

- [ ] **Step 2: 全テスト緑を確認**

Run: `npm test`
Expected: PASS（全件）

- [ ] **Step 3: README・進捗ログに追記**

`README.md` に BGM/効果音（Web Audio・課金ゼロ・🔊トグル/音量・端末記憶）の節を1つ追加。
`docs/進捗.md` の先頭 `最終更新` を更新し、「BGM/効果音 追加（sw v1.11.0）」のエントリを追記（3モジュール構成・設計/計画パス・課金ゼロ・実機実聴確認）。

- [ ] **Step 4: マージ・公開・live確認**

```bash
cd /d/Desktop-Archive/manabi-quest
git add sw.js README.md docs/進捗.md
git commit -m "chore: sw v1.11.0(audio 3ファイル追加)・README/進捗 追記"
git checkout master
git merge --no-ff feat/bgm -m "merge: BGM/効果音(Web Audio・3曲+SE・課金ゼロ)"
git push origin master
```

数分後、live で確認（キャッシュバスター付き）:
- `https://takagifamilyclinic-source.github.io/manabi-quest/sw.js` が `v1.11.0`。
- `https://takagifamilyclinic-source.github.io/manabi-quest/js/audio.js` が 200 で配信。
- 実iPad（Safari→ホーム画面のアプリ）で**実際に音を聴いて**確認（BGM3曲・SE・🔊トグル・音量）。

- [ ] **Step 5: メモリ更新**

`C:\Users\User\.claude\projects\...\memory\manabi-quest-app.md` に「BGM/効果音 追加（sw v1.11.0・Web Audio・課金ゼロ・3モジュール・端末設定 manabi-audio）」を追記。
```

## Self-Review

**1. Spec coverage:** 音の雰囲気(Task2 データ=8bit矩形波melody+丸い伴奏)／3曲(Task2)／SE(Task2/4)／初期ON・🔊トグル・音量(Task4)／端末記憶=manabi-audio(Task3)／初回タップ解錠(Task4)／省電力(Task4)／英語🔊両立(Task4確認)／スキーマ非変更(Task3=別キー)／sw版上げ(Task5)／テスト(Task1/2純ロジック＋ブラウザ)／変更ファイル一覧(全Task) — すべて対応タスクあり。

**2. Placeholder scan:** TBD/TODO・「適切に」等なし。コードは実物を記載。

**3. Type consistency:** `playTrack('field'|'battle')`・`playJingle('victory',cb)`・`playSfx(name)`・`setMuted/isMuted`・`setVolume/getVolume`・`trackForScreen`・`noteToFreq`・`clampVolume`・`parseAudioSettings`・`TRACKS/SFX`・`{n,d}`・`manabi-audio` キー — Task間で一致。Task3のエンジンは `startSong`/`scheduleTick`/`songDuration` の単一実装（重複関数なし・`st.wave`/`st.dest` 参照）。`voiceState` の各声部が `wave`/`dest` を保持し `scheduleTick` と整合。`SFX.d` は秒（`playSfx` が beatSec=1 で再生）と明記。
